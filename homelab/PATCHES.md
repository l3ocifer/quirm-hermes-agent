# Local patches vs upstream NousResearch/hermes-agent

Track non-additive changes (anything outside `homelab/`).

## Active patches

_(none today — Quirm ships pure-vanilla Hermes, customized only via
configs in `homelab/config/`. Quirm's own work — eval harness,
benchmark scripts — lives in `quirm-graph/pages/`, not in source code.)_
